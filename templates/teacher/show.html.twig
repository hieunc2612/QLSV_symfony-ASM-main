{% extends 'base.html.twig' %}

{% block title %}Teacher{% endblock %}

{% block body %}
    <div class=" table container px-5">
    <h1>Teacher Information</h1>
    <table class="table  table-bordered container text-center px-5 ">
        <tbody>
            <tr>
                <th class="table-primary">Id</th>
                <td>{{ teacher.id }}</td>
            </tr>
            <tr>
                <th class="table-info">Name</th>
                <td>{{ teacher.Name }}</td>
            </tr>
            <tr>
                <th class="table-primary">Phone</th>
                <td>{{ teacher.Phone }}</td>
            </tr>
        </tbody>
    </table>
    <div class=" gap-2 col-2 mx-auto d-md-block" >
    <a class="btn btn-info" style="margin:auto; display:block" href="{{ path('app_teacher_index') }}">Back to list</a> 
    </div>
     <div class="py-1">
    {{ include('teacher/_delete_form.html.twig') }}
     </div>
     </div>
{% endblock %}
