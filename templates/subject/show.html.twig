{% extends 'base.html.twig' %}

{% block title %}Subject{% endblock %}

{% block body %}
    <h1>Subject Infomation</h1>
    <div class=" table container px-5">
    <table class="table table-bordered container text-center px-5 ">
        <tbody>
        <tbody>
            <tr>
                <th class="table-primary">Id</th>
                <td>{{ subject.id }}</td>
            </tr>
            <tr>
                <th class="table-info">Name</th>
                <td>{{ subject.Name }}</td>
            </tr>
            <tr>
                <th class="table-primary">Teacher</th>
                <td>{{ subject.Teacher.Name }}</td>
            </tr>
        </tbody>
    </table>
    <div class=" gap-2 col-2 mx-auto d-md-block" >
    <a class="btn btn-info" style="margin:auto; display:block" href="{{ path('app_subject_index') }}">Back to list</a>
     </div>
     <div class="py-1">
    {{ include('subject/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
